version: "3.8"

services:
    bot:
        build: .
        container_name: xobot
        environment:
            BOT_TOKEN: ${BOT_TOKEN}
        volumes:
            - ./src:/app/src
        restart: unless-stopped
